/// Application-wide constants for SightSense AI.
class AppConstants {
  AppConstants._();

  // Application
  static const String appName = 'SightSense AI';
  static const String tagline = 'Your AI Companion for Independent Living';

  // Animation
  static const Duration defaultAnimationDuration = Duration(milliseconds: 300);

  // Spacing
  static const double defaultPadding = 16.0;
  static const double smallPadding = 8.0;
  static const double largePadding = 24.0;
  static const double buttonVerticalPadding = 12.0;
  static const double inputVerticalPadding = 14.0;

  // Radius
  static const double defaultRadius = 12.0;
  static const double largeRadius = 20.0;

  // API
  static const String geminiModel = 'gemini-2.5-flash';
}
